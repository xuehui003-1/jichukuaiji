'use client';
import {useEffect,useState} from 'react';
type S={mode:string;provider:string;model:string;configured:boolean;total:number;success:number;failed:number;successRate:number;averageDurationMs:number;errors:Record<string,number>;recent:Array<any>};
export default function AIStatusPanel(){const [s,setS]=useState<S|null>(null);async function load(){const r=await fetch('/api/ai/status',{cache:'no-store'}),d=await r.json();if(d.ok)setS(d.status)}useEffect(()=>{load()},[]);if(!s)return <section className="ai-status"><p>正在读取AI运行状态…</p></section>;const online=s.mode==='online'&&s.configured;return <section className="ai-status" id="ai-status"><div className="ais-head"><div><span>人机协同引擎</span><h2>规则、DeepSeek与教师复核</h2><p>AI运行状态可追踪，但API Key永不显示</p></div><div className={online?'online':'offline'}><i/>{online?'DeepSeek在线':'离线规则模式'}</div></div><div className="ai-pipeline"><article><b>01</b><span>确定性规则引擎</span><small>金额、关系方向、会计要素</small></article><i>→</i><article className={online?'active':''}><b>02</b><span>{s.provider} · {s.model}</span><small>语义解析、追问与变式</small></article><i>→</i><article><b>03</b><span>教师最终复核</span><small>低置信度、冲突和开放场景</small></article></div><div className="ai-metrics"><div><span>调用次数</span><b>{s.total}</b></div><div><span>成功率</span><b>{s.total?s.successRate:'—'}{s.total?'%':''}</b></div><div><span>平均耗时</span><b>{s.total?s.averageDurationMs:'—'}{s.total?'ms':''}</b></div><div><span>降级/失败</span><b>{s.failed}</b></div></div>{!online&&<div className="ai-notice">当前未启用真实API。双击 <code>configure-deepseek.cmd</code> 配置后，重启两个应用。</div>}{s.recent.length>0&&<div className="ai-recent"><b>最近调用</b>{s.recent.slice(0,4).map((x,i)=><span key={i}><i className={x.status==='SUCCESS'?'ok':'fail'}/>{x.requestType}<em>{x.durationMs}ms</em></span>)}</div>}<button onClick={load}>刷新AI状态</button></section>}
